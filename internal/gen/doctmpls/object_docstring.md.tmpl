`{{ .ObjectName }}` represents the `{{ .ProviderName }}_{{ .ObjectName }}` Terraform {{ .ResourceOrDataSource }}.

{{ .Description }}

This package contains functions and utilities for setting up the {{ .ResourceOrDataSource }} using Jsonnet code.
